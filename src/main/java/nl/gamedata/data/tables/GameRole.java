/*
 * This file is generated by jOOQ.
 */
package nl.gamedata.data.tables;


import java.util.Arrays;
import java.util.Collection;
import java.util.List;

import nl.gamedata.data.Gamedata;
import nl.gamedata.data.Indexes;
import nl.gamedata.data.Keys;
import nl.gamedata.data.tables.Game.GamePath;
import nl.gamedata.data.tables.User.UserPath;
import nl.gamedata.data.tables.records.GameRoleRecord;

import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Select;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class GameRole extends TableImpl<GameRoleRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>gamedata.game_role</code>
     */
    public static final GameRole GAME_ROLE = new GameRole();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<GameRoleRecord> getRecordType() {
        return GameRoleRecord.class;
    }

    /**
     * The column <code>gamedata.game_role.id</code>.
     */
    public final TableField<GameRoleRecord, Integer> ID = createField(DSL.name("id"), SQLDataType.INTEGER.nullable(false).identity(true), this, "");

    /**
     * The column <code>gamedata.game_role.view</code>.
     */
    public final TableField<GameRoleRecord, Byte> VIEW = createField(DSL.name("view"), SQLDataType.TINYINT.nullable(false).defaultValue(DSL.field(DSL.raw("0"), SQLDataType.TINYINT)), this, "");

    /**
     * The column <code>gamedata.game_role.edit</code>.
     */
    public final TableField<GameRoleRecord, Byte> EDIT = createField(DSL.name("edit"), SQLDataType.TINYINT.nullable(false).defaultValue(DSL.field(DSL.raw("0"), SQLDataType.TINYINT)), this, "");

    /**
     * The column <code>gamedata.game_role.user_id</code>.
     */
    public final TableField<GameRoleRecord, Integer> USER_ID = createField(DSL.name("user_id"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>gamedata.game_role.game_id</code>.
     */
    public final TableField<GameRoleRecord, Integer> GAME_ID = createField(DSL.name("game_id"), SQLDataType.INTEGER.nullable(false), this, "");

    private GameRole(Name alias, Table<GameRoleRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private GameRole(Name alias, Table<GameRoleRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>gamedata.game_role</code> table reference
     */
    public GameRole(String alias) {
        this(DSL.name(alias), GAME_ROLE);
    }

    /**
     * Create an aliased <code>gamedata.game_role</code> table reference
     */
    public GameRole(Name alias) {
        this(alias, GAME_ROLE);
    }

    /**
     * Create a <code>gamedata.game_role</code> table reference
     */
    public GameRole() {
        this(DSL.name("game_role"), null);
    }

    public <O extends Record> GameRole(Table<O> path, ForeignKey<O, GameRoleRecord> childPath, InverseForeignKey<O, GameRoleRecord> parentPath) {
        super(path, childPath, parentPath, GAME_ROLE);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class GameRolePath extends GameRole implements Path<GameRoleRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> GameRolePath(Table<O> path, ForeignKey<O, GameRoleRecord> childPath, InverseForeignKey<O, GameRoleRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private GameRolePath(Name alias, Table<GameRoleRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public GameRolePath as(String alias) {
            return new GameRolePath(DSL.name(alias), this);
        }

        @Override
        public GameRolePath as(Name alias) {
            return new GameRolePath(alias, this);
        }

        @Override
        public GameRolePath as(Table<?> alias) {
            return new GameRolePath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Gamedata.GAMEDATA;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.GAME_ROLE_FK_GAME_ROLE_GAME1_IDX, Indexes.GAME_ROLE_FK_GAME_ROLE_USER_IDX);
    }

    @Override
    public Identity<GameRoleRecord, Integer> getIdentity() {
        return (Identity<GameRoleRecord, Integer>) super.getIdentity();
    }

    @Override
    public UniqueKey<GameRoleRecord> getPrimaryKey() {
        return Keys.KEY_GAME_ROLE_PRIMARY;
    }

    @Override
    public List<UniqueKey<GameRoleRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.KEY_GAME_ROLE_ID_UNIQUE);
    }

    @Override
    public List<ForeignKey<GameRoleRecord, ?>> getReferences() {
        return Arrays.asList(Keys.FK_GAME_ROLE_GAME1, Keys.FK_GAME_ROLE_USER);
    }

    private transient GamePath _game;

    /**
     * Get the implicit join path to the <code>gamedata.game</code> table.
     */
    public GamePath game() {
        if (_game == null)
            _game = new GamePath(this, Keys.FK_GAME_ROLE_GAME1, null);

        return _game;
    }

    private transient UserPath _user;

    /**
     * Get the implicit join path to the <code>gamedata.user</code> table.
     */
    public UserPath user() {
        if (_user == null)
            _user = new UserPath(this, Keys.FK_GAME_ROLE_USER, null);

        return _user;
    }

    @Override
    public GameRole as(String alias) {
        return new GameRole(DSL.name(alias), this);
    }

    @Override
    public GameRole as(Name alias) {
        return new GameRole(alias, this);
    }

    @Override
    public GameRole as(Table<?> alias) {
        return new GameRole(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public GameRole rename(String name) {
        return new GameRole(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public GameRole rename(Name name) {
        return new GameRole(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public GameRole rename(Table<?> name) {
        return new GameRole(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public GameRole where(Condition condition) {
        return new GameRole(getQualifiedName(), aliased() ? this : null, null, condition);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public GameRole where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public GameRole where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public GameRole where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public GameRole where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public GameRole where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public GameRole where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public GameRole where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public GameRole whereExists(Select<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public GameRole whereNotExists(Select<?> select) {
        return where(DSL.notExists(select));
    }
}
